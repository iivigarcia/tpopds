package com.uade.tpo.model.state;

public class Finalizado implements EstadoPartido {
  // Es un estado terminal. No se permite ninguna operación.
  // Todas las llamadas a métodos usarán la implementación predeterminada de la
  // interfaz,
  // que lanza IllegalStateException.
}